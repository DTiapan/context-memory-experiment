[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "context-memory-experiment"
version = "0.1.0"
description = "Small experiments for indexed and adaptive LLM memory retrieval"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
llm = ["openai>=1.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
